CREATE TABLE `health_exam_result`.`xml_ledger` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `event_id` bigint NOT NULL,
  `subscriber_id` bigint unsigned DEFAULT NULL,
  `hia_subscriber_id` varchar(190) DEFAULT NULL,
  `xml_sha256` char(64) NOT NULL,
  `document_id` varchar(190) DEFAULT NULL,
  `insurer_number` varchar(20) DEFAULT NULL,
  `facility_code` varchar(64) DEFAULT NULL,
  `facility_name` varchar(255) DEFAULT NULL,
  `exam_date` date DEFAULT NULL,
  `name_kana_raw` varchar(255) DEFAULT NULL,
  `name_kana_match` varchar(255) DEFAULT NULL,
  `insurance_symbol_raw` varchar(190) DEFAULT NULL,
  `insurance_symbol_match` varchar(190) DEFAULT NULL,
  `insurance_number_raw` varchar(190) DEFAULT NULL,
  `insurance_number_match` varchar(190) DEFAULT NULL,
  `birthdate` date DEFAULT NULL,
  `gender_code` varchar(16) DEFAULT NULL,
  `identity_hash` char(64) DEFAULT NULL,
  `person_id_custom` varchar(190) DEFAULT NULL,
  `subscriber_match_status` varchar(32) DEFAULT NULL,
  `subscriber_match_method` varchar(64) DEFAULT NULL,
  `subscriber_match_reason` text,
  `xml_status` varchar(32) NOT NULL DEFAULT 'PENDING',
  `xml_reason` text,
  `check_status` varchar(32) NOT NULL DEFAULT 'PENDING',
  `xml_export_status` varchar(32) NOT NULL DEFAULT 'PENDING',
  `manual_export_approved` tinyint(1) NOT NULL DEFAULT 0,
  `manual_export_reason` text,
  `created_at` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),

  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_xml_ledger_xml_sha256` (`xml_sha256`),
  KEY `idx_xml_ledger_event` (`event_id`),
  KEY `idx_xml_ledger_subscriber` (`subscriber_id`),
  KEY `idx_xml_ledger_hia_subscriber` (`hia_subscriber_id`),
  KEY `idx_xml_ledger_document` (`document_id`),
  KEY `idx_xml_ledger_insurer_number` (`insurer_number`),
  KEY `idx_xml_ledger_facility` (`facility_code`),
  KEY `idx_xml_ledger_exam_date` (`exam_date`),
  KEY `idx_xml_ledger_birthdate` (`birthdate`),
  KEY `idx_xml_ledger_identity_hash` (`identity_hash`),
  KEY `idx_xml_ledger_person_id_custom` (`person_id_custom`),
  KEY `idx_xml_ledger_subscriber_match_status` (`subscriber_match_status`),
  KEY `idx_xml_ledger_xml_status` (`xml_status`),
  KEY `idx_xml_ledger_check_status` (`check_status`),
  KEY `idx_xml_ledger_export_status` (`xml_export_status`),
  KEY `idx_xml_ledger_manual_export` (`manual_export_approved`)
)
ENGINE=InnoDB
DEFAULT CHARSET=utf8mb4
COLLATE=utf8mb4_ja_0900_as_cs;
